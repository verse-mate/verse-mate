{
  "name": "frontend-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --bun run next dev --turbo",
    "build": "bun --bun run next build",
    "build:worker": "bun run build && bun run prepare:worker",
    "prepare:worker": "cp -r .next/static .cf/public/_next/static && cp -r public/* .cf/public/",
    "start": "bun --bun run next start",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "frontend-base": "workspace:*",
    "frontend-envs": "workspace:*",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.13",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "postcss-nested": "^6.2.0",
    "typescript": "^5.5.4"
  }
}
